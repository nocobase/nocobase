# nb env

Gestiona los env guardados de NocoBase CLI. Un env guarda información de conexión y runtime local, como la dirección de la API, la autenticación, la ruta local de la app y la configuración de la base de datos.

## Ejemplo

```bash
nb env proxy nginx --env app1
```
