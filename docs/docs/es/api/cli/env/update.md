# nb env update

`nb env update` actualiza la configuración guardada de un env. Ahora también puedes ajustar `app-public-path` y `cdn-base-url`, y después volver a ejecutar `nb env proxy nginx` o `nb env proxy caddy` cuando esos cambios afecten al proxy.
