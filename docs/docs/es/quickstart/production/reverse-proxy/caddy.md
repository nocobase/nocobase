# Caddy

Si ya tienes un dominio y quieres activar HTTPS rápidamente, `nb env proxy caddy` suele ser la opción más simple. Si prefieres mantener todo el `Caddyfile` a mano, usa una configuración mínima con `reverse_proxy`.
