:::tip
이 문서는 AI로 번역되었습니다. 부정확한 내용이 있을 경우 [영어 버전](/en)을 참조하세요
:::

## 매개변수

| 매개변수 이름          | 타입          | 기본값 | 설명                                                |
| :--------------------- | :------------ | :----- | :-------------------------------------------------- |
| `options.values`       | `M`           | `{}`   | 삽입할 데이터 객체입니다.                           |
| `options.whitelist?`   | `string[]`    | -      | `values` 필드의 화이트리스트입니다. 목록에 있는 필드만 저장됩니다. |
| `options.blacklist?`   | `string[]`    | -      | `values` 필드의 블랙리스트입니다. 목록에 있는 필드는 저장되지 않습니다. |
| `options.transaction?` | `Transaction` | -      | 트랜잭션입니다.                                     |