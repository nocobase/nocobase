## BelongsToRepository

La sua interfaccia è identica a quella di [HasOneRepository](./has-one-repository.md).
`BelongsToRepository` è un `Repository` per la gestione delle relazioni `BelongsTo`. Offre alcuni metodi pratici per gestire queste relazioni.