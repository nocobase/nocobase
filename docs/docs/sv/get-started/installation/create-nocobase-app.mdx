:::tip
Detta dokument har översatts av AI. För eventuella felaktigheter, se [den engelska versionen](/en)
:::


# create-nocobase-app Installation

:::tip Förutsättningar

- Node.js 20+ och Yarn 1.22.x är installerade.
- En av de nödvändiga databaserna är konfigurerad och körs: MySQL 8.0.17+, MariaDB 10.9+ eller PostgreSQL 10+.

:::

## 1. Skapa ett NocoBase-projekt

Välj NocoBase-version ([Versionsjämförelse](../quickstart.md)) och databastyp, kör sedan motsvarande kommando.

<Tabs>
<Tab label="senaste versionen">

<Tabs>
<Tab label="PostgreSQL">

```bash
yarn create nocobase-app my-nocobase-app -d postgres \
   -e DB_HOST=localhost \
   -e DB_PORT=5432 \
   -e DB_DATABASE=nocobase \
   -e DB_USER=nocobase \
   -e DB_PASSWORD=nocobase \
   -e TZ=Asia/Shanghai
```

</Tab>

<Tab label="MySQL">

```bash
yarn create nocobase-app my-nocobase-app -d mysql \
   -e DB_HOST=localhost \
   -e DB_PORT=3306 \
   -e DB_DATABASE=nocobase \
   -e DB_USER=nocobase \
   -e DB_PASSWORD=nocobase \
   -e TZ=Asia/Shanghai
```

</Tab>

<Tab label="MariaDB">

```bash
yarn create nocobase-app my-nocobase-app -d mariadb \
   -e DB_HOST=localhost \
   -e DB_PORT=3306 \
   -e DB_DATABASE=nocobase \
   -e DB_USER=nocobase \
   -e DB_PASSWORD=nocobase \
   -e TZ=Asia/Shanghai
```

</Tab>
</Tabs>

</Tab>

<Tab label="betaversion">

<Tabs>
<Tab label="PostgreSQL">

```bash
npx create-nocobase-app@beta my-nocobase-app -d postgres \
   -e DB_HOST=localhost \
   -e DB_PORT=5432 \
   -e DB_DATABASE=nocobase \
   -e DB_USER=nocobase \
   -e DB_PASSWORD=nocobase \
   -e TZ=Asia/Shanghai
```

</Tab>

<Tab label="MySQL">

```bash
npx create-nocobase-app@beta my-nocobase-app -d mysql \
   -e DB_HOST=localhost \
   -e DB_PORT=3306 \
   -e DB_DATABASE=nocobase \
   -e DB_USER=nocobase \
   -e DB_PASSWORD=nocobase \
   -e TZ=Asia/Shanghai
```

</Tab>

<Tab label="MariaDB">

```bash
npx create-nocobase-app@beta my-nocobase-app -d mariadb \
   -e DB_HOST=localhost \
   -e DB_PORT=3306 \
   -e DB_DATABASE=nocobase \
   -e DB_USER=nocobase \
   -e DB_PASSWORD=nocobase \
   -e TZ=Asia/Shanghai
```

</Tab>
</Tabs>

</Tab>

<Tab label="alphaversion">

<Tabs>
<Tab label="PostgreSQL">

```bash
npx create-nocobase-app@alpha my-nocobase-app -d postgres \
   -e DB_HOST=localhost \
   -e DB_PORT=5432 \
   -e DB_DATABASE=nocobase \
   -e DB_USER=nocobase \
   -e DB_PASSWORD=nocobase \
   -e TZ=Asia/Shanghai
```

</Tab>

<Tab label="MySQL">

```bash
npx create-nocobase-app@alpha my-nocobase-app -d mysql \
   -e DB_HOST=localhost \
   -e DB_PORT=3306 \
   -e DB_DATABASE=nocobase \
   -e DB_USER=nocobase \
   -e DB_PASSWORD=nocobase \
   -e TZ=Asia/Shanghai
```

</Tab>

<Tab label="MariaDB">

```bash
npx create-nocobase-app@alpha my-nocobase-app -d mariadb \
   -e DB_HOST=localhost \
   -e DB_PORT=3306 \
   -e DB_DATABASE=nocobase \
   -e DB_USER=nocobase \
   -e DB_PASSWORD=nocobase \
   -e TZ=Asia/Shanghai
```

</Tab>
</Tabs>

</Tab>
</Tabs>

:::warning Miljövariabler

- `TZ` används för att ställa in applikationens tidszon, som standard är operativsystemets tidszon.
- `APP_KEY` är applikationens hemliga nyckel, som används för att generera användartokens m.m. (Om `APP_KEY` ändras blir gamla tokens ogiltiga). Det kan vara vilken slumpmässig sträng som helst. **Ändra den till er egen hemliga nyckel och se till att den inte exponeras.**
- `DB_*` är databasrelaterade konfigurationer. Ändra dem så att de matchar er databasanslutningsinformation enligt er faktiska installation.

:::

## 2. Byt katalog

```bash
cd my-nocobase-app
```

## 3. Installera beroenden

📢 Beroende på er nätverksmiljö, systemkonfiguration och andra faktorer kan det här steget ta över tio minuter.

```bash
yarn install
```

:::tip Tips för produktionsmiljö

När ni driftsätter i en produktionsmiljö kan ni installera endast de nödvändiga beroendena för att minska storleken:

```bash
yarn install --production
```

:::

## 4. Installera NocoBase

```bash
yarn nocobase install --lang=zh-CN
```

Installationsprocessen kommer automatiskt att skapa databastabellstrukturen och initiera data.

## 5. Starta NocoBase

**Utvecklingsmiljö**

```bash
yarn dev
```

**Produktionsmiljö**

```bash
yarn start
```

## 6. Logga in på NocoBase

Öppna [http://localhost:13000](http://localhost:13000) i er webbläsare. Det ursprungliga kontot och lösenordet är `admin@nocobase.com` och `admin123`.

:::warning Tips för kontosäkerhet

Efter er första inloggning, vänligen ändra standardlösenordet omedelbart för att säkerställa systemsäkerheten.

:::