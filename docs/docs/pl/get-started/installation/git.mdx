:::tip
Ten dokument został przetłumaczony przez AI. W przypadku niedokładności, proszę odnieść się do [wersji angielskiej](/en)
:::


# Instalacja z kodu źródłowego Git

:::tip Wymagania wstępne

- Zainstalowane Git, Node.js 20+ oraz Yarn 1.22.x
- Skonfigurowana i uruchomiona jedna z wymaganych baz danych: MySQL 8.0.17+, MariaDB 10.9+ lub PostgreSQL 10+
- Ze względu na specyfikę sieci, zalecamy (choć nie jest to obowiązkowe) użycie krajowego lustra pakietów.

```bash
$ yarn config set disable-self-update-check true
$ yarn config set registry https://registry.npmmirror.com/
```

:::

## 1. Pobieranie NocoBase na komputer

Proszę wybrać wersję NocoBase ([Porównanie wersji](../quickstart.md)) i wykonać odpowiednie polecenie.

<Tabs>
<Tab label="najnowsza wersja">

```bash
git clone https://github.com/nocobase/nocobase.git -b main --depth=1 my-nocobase
```

</Tab>
<Tab label="wersja Beta">

```bash
git clone https://github.com/nocobase/nocobase.git -b next --depth=1 my-nocobase
```

</Tab>
<Tab label="wersja Alpha">

```bash
git clone https://github.com/nocobase/nocobase.git -b develop --depth=1 my-nocobase
```

</Tab>
</Tabs>

## 2. Zmiana katalogu

```bash
cd my-nocobase
```

## 3. Instalacja zależności

📢 Ten krok może potrwać kilkanaście minut, w zależności od Państwa środowiska sieciowego, konfiguracji systemu i innych czynników.

```bash
yarn install --frozen-lockfile
```

## 4. Ustawianie zmiennych środowiskowych

Zmienne środowiskowe wymagane przez NocoBase są przechowywane w pliku `.env` w katalogu głównym. Proszę zmodyfikować je zgodnie z Państwa rzeczywistą konfiguracją.

```bash
TZ=Asia/Shanghai
APP_KEY=your-secret-key
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=postgres
DB_USER=nocobase
DB_PASSWORD=nocobase
```

:::warning Opis zmiennych środowiskowych

- `TZ` służy do ustawienia strefy czasowej aplikacji; domyślnie jest to strefa czasowa systemu operacyjnego.
- `APP_KEY` to klucz tajny aplikacji, używany do generowania tokenów użytkowników itp. (Jeśli `APP_KEY` zostanie zmieniony, stare tokeny staną się nieważne). Może to być dowolny losowy ciąg znaków. Proszę zmienić go na własny klucz tajny i upewnić się, że nie zostanie on ujawniony.
- `DB_*` to zmienne związane z bazą danych. Jeśli nie używają Państwo domyślnej usługi bazy danych z przykładu, proszę zmodyfikować je zgodnie z Państwa rzeczywistą konfiguracją.

:::

## 5. Instalacja NocoBase

```bash
yarn nocobase install --lang=pl-PL
```

## 6. Uruchamianie NocoBase

**Środowisko deweloperskie**

```bash
yarn dev
```

**Środowisko produkcyjne (niezalecane)**

Nie zaleca się bezpośredniego wdrażania z kodu źródłowego w środowisku produkcyjnym. W przypadku środowiska produkcyjnego, proszę zapoznać się z dokumentacją [Wdrożenie produkcyjne](../deployment/production.md).

```bash
yarn build
yarn start
```

## 7. Logowanie do NocoBase

Proszę otworzyć [http://localhost:13000](http://localhost:13000) w przeglądarce. Początkowe dane logowania to `admin@nocobase.com` (konto) i `admin123` (hasło).

:::warning Ostrzeżenie dotyczące bezpieczeństwa konta

Po pierwszym zalogowaniu proszę niezwłocznie zmienić domyślne hasło, aby zapewnić bezpieczeństwo systemu.

:::