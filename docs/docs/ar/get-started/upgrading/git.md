:::tip إشعار الترجمة بالذكاء الاصطناعي
تمت ترجمة هذه الوثائق تلقائيًا بواسطة الذكاء الاصطناعي.
:::

# ترقية تثبيت NocoBase من كود المصدر (Git)

:::warning التحضير قبل الترقية

- تأكد من عمل نسخة احتياطية لقاعدة البيانات أولاً.
- أوقف تشغيل NocoBase الحالي (`Ctrl + C`).

:::

## 1. الانتقال إلى دليل مشروع NocoBase

```bash
cd my-nocobase-app
```

## 2. سحب أحدث الكود

```bash
git pull
```

## 3. حذف ذاكرة التخزين المؤقت والتبعيات القديمة (اختياري)

إذا فشلت عملية الترقية المعتادة، يمكنك محاولة مسح ذاكرة التخزين المؤقت والتبعيات ثم إعادة تنزيلها.

```bash
# مسح ذاكرة التخزين المؤقت لـ NocoBase
yarn nocobase clean
# حذف التبعيات
yarn rimraf -rf node_modules # مكافئ لـ rm -rf node_modules
```

## 4. تحديث التبعيات

📢 نظرًا لعوامل مثل بيئة الشبكة وتكوين النظام، قد تستغرق هذه الخطوة التالية أكثر من عشر دقائق.

```bash
yarn install
```

## 5. تشغيل أمر الترقية

```bash
yarn nocobase upgrade
```

## 6. تشغيل NocoBase

```bash
yarn dev
```

:::tip نصيحة لبيئة الإنتاج

لا يُنصح بنشر تثبيت NocoBase من كود المصدر مباشرةً في بيئة إنتاج (لبيئات الإنتاج، يُرجى الرجوع إلى [نشر الإنتاج](../deployment/production.md)).

:::

## 7. ترقية الإضافات الخارجية

راجع [تثبيت وترقية الإضافات](../install-upgrade-plugins.mdx)