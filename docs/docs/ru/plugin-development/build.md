# Сборка

### Включение плагина по умолчанию

После загрузки плагин не активируется автоматически — он появится в «Менеджере плагинов» и его потребуется включить вручную. Если вы поддерживаете собственное приложение NocoBase и хотите, чтобы плагин активировался вместе с приложением по умолчанию, можно использовать переменную среды `APPEND_PRESET_BUILT_IN_PLUGINS` (добавить встроенные плагины). Подробнее см. в разделе [Предустановка и включение плагина по умолчанию](./write-your-first-plugin.md#предустановка-и-включение-плагина-по-умолчанию-необязательно).

## Связанные ссылки

- [Написание первого плагина](./write-your-first-plugin.md) — создание плагина с нуля, включая полный процесс сборки и упаковки
- [Структура проекта](./project-structure.md) — назначение каталогов `packages/plugins`, `storage/tar` и других
- [Управление зависимостями](./dependency-management.md) — объявление зависимостей плагина и глобальные зависимости
- [Обзор разработки плагинов](./index.md) — общее введение в разработку плагинов
- [Установка и обновление плагинов](../get-started/install-upgrade-plugins.mdx) — загрузка упакованного плагина в целевую среду
- [Переменные среды](../get-started/installation/env.md) — настройка переменных среды для предустановленных и встроенных плагинов

## Пользовательская конфигурация сборки

Если вы хотите настроить конфигурацию сборки, создайте файл `build.config.ts` в корневой директории плагина со следующим содержимым:

```js
import { defineConfig } from '@nocobase/build';

export default defineConfig({
  modifyRsbuildConfig: (config) => {
    // Rsbuild используется для сборки кода клиентской части (`src/client`)

    // Измените конфигурацию Rsbuild. Подробнее см.: https://rsbuild.rs/guide/configuration/rsbuild
    return config
  },
  modifyTsupConfig: (config) => {
    // tsup используется для сборки кода серверной части (`src/server`)

    // Изменение конфигурации tsup: https://tsup.egoist.dev/#using-custom-configuration
    return config
  },
  beforeBuild: (log) => {
    // Функция обратного вызова, выполняющаяся перед началом сборки; можно запускать предварительные операции.
  },
  afterBuild: (log: PkgLog) => {
    // Функция обратного вызова, выполняющаяся после завершения сборки; можно запускать пост-обработку.
  };
});
```
