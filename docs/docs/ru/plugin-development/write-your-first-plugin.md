---
title: "Создайте свой первый плагин NocoBase"
description: "Создание плагина-блока с нуля: nb scaffold plugin, шаблон плагина, каталоги client/server, регистрация блока, процесс разработки и отладки."
keywords: "создание плагина,первый плагин,nb scaffold plugin,шаблон плагина,плагин-блок,разработка плагинов NocoBase"
---

# Создайте свой первый плагин

В этом руководстве мы шаг за шагом создадим плагин-блок, который можно будет использовать на страницах. Это поможет вам понять базовую структуру и процесс разработки плагинов NocoBase.

## Предварительные условия

Прежде чем начать, убедитесь, что вы установили приложение NocoBase с помощью NocoBase CLI (`nb init`). CLI поддерживает два источника — npm и Git; рекомендуется использовать Git (при разработке с ИИ можно напрямую обращаться к исходному коду). Подробнее см. [Установка приложения через CLI](../nocobase-cli/installation/cli.md) или [Руководство по подключению AI Agent](../ai/quick-start.mdx).

После завершения установки вы сможете официально начать свой путь в разработке плагинов.

## Шаг 1: Создание основы плагина с помощью CLI

В корневом каталоге проекта (`<app-path>`) или каталоге `source/` выполните следующую команду, чтобы быстро сгенерировать пустой плагин:

```bash
nb scaffold plugin @my-project/plugin-hello
```

После успешного выполнения команды в каталоге `<app-path>/plugins/@my-project/plugin-hello` будут сгенерированы базовые файлы (`nb` автоматически синхронизирует плагин в `source/packages/plugins/` для разработки и сборки). Структура по умолчанию выглядит так:

```bash
├─ /plugins/@my-project/plugin-hello
  ├─ package.json
  ├─ README.md
  ├─ client-v2.d.ts
  ├─ client-v2.js
  ├─ server.d.ts
  ├─ server.js
  └─ src
     ├─ index.ts                 # Экспорт плагина для серверной части по умолчанию
     ├─ client-v2                 # Расположение кода для клиентской части
     │  ├─ index.tsx             # Класс плагина для клиентской части, экспортируемый по умолчанию
     │  ├─ plugin.tsx            # Точка входа плагина (наследует @nocobase/client-v2 Plugin)
     │  ├─ locale.ts             # Утилиты перевода useT / tExpr
     │  ├─ models                # Опционально: модели для фронтенда (например, узлы рабочего процесса)
     │  │  └─ index.ts
     │  └─ utils
     │     ├─ index.ts
     │     └─ useT.ts
     ├─ server                   # Расположение кода для серверной части
     │  ├─ index.ts              # Класс плагина для серверной части, экспортируемый по умолчанию
     │  ├─ plugin.ts             # Точка входа плагина (наследует @nocobase/server Plugin)
     │  ├─ collections           # Опционально: коллекции для серверной части
     │  ├─ migrations            # Опционально: миграции данных
     │  └─ utils
     │     └─ index.ts
     ├─ utils
     │  ├─ index.ts
     │  └─ tExpr.ts
     └─ locale                   # Опционально: мультиязычность
        ├─ en-US.json
        └─ zh-CN.json
```

После создания вы можете открыть страницу менеджера плагинов в браузере (адрес по умолчанию: http://localhost:13000/admin/settings/plugin-manager), чтобы убедиться, что плагин появился в списке.

## Шаг 2: Реализация простого клиентского блока

Далее мы добавим в плагин пользовательскую модель блока, которая будет отображать приветственный текст.

1. **Создайте новый файл модели блока** `client-v2/models/HelloBlockModel.tsx`:

```tsx pure
import { BlockModel } from '@nocobase/client-v2';
import React from 'react';
import { tExpr } from '../utils';

export class HelloBlockModel extends BlockModel {
  renderComponent() {
    return (
      <div>
        <h1>Hello, NocoBase!</h1>
        <p>This is a simple block rendered by HelloBlockModel.</p>
      </div>
    );
  }
}

HelloBlockModel.define({
  label: tExpr('Hello block'),
});
```

2. **Зарегистрируйте модель блока**. Отредактируйте `client-v2/models/index.ts`, чтобы экспортировать новую модель для загрузки во время выполнения на фронтенде:

```ts
import { ModelConstructor } from '@nocobase/flow-engine';
import { HelloBlockModel } from './HelloBlockModel';

export default {
  HelloBlockModel,
} as Record<string, ModelConstructor>;
```

После сохранения кода, если вы используете скрипт для разработки, вы должны увидеть логи горячей перезагрузки в выводе терминала.

## Шаг 3: Активация и тестирование плагина

Вы можете включить плагин через командную строку или интерфейс:

- **Командная строка**

  ```bash
  nb plugin enable @my-project/plugin-hello
  ```

- **Интерфейс управления**: Откройте менеджер плагинов, найдите `@my-project/plugin-hello` и нажмите «Активировать».

После активации создайте новую страницу «Modern page (v2)». При добавлении блоков вы увидите «Hello block». Вставьте его на страницу, чтобы увидеть приветственное содержимое, которое вы только что написали.

![20250928174529](https://static-docs.nocobase.com/20250928174529.png)

### Предустановка и включение плагина по умолчанию (необязательно)

Выше описан способ вручную включить отдельный плагин. Если вы поддерживаете собственное приложение NocoBase и хотите, чтобы определённые плагины были автоматически готовы к работе после выполнения `nocobase install` (первоначальная установка) или `nocobase upgrade` (обновление), можно использовать две переменные среды для управления состоянием плагинов по умолчанию:

- **`APPEND_PRESET_LOCAL_PLUGINS` (добавить предустановленные плагины)** — добавляет плагин в список предустановленных локальных плагинов; после установки он появится в «Менеджере плагинов», но по умолчанию не будет активирован — потребуется включить его вручную
- **`APPEND_PRESET_BUILT_IN_PLUGINS` (добавить встроенные плагины)** — добавляет плагин в список встроенных плагинов; при установке он активируется автоматически, и как встроенный плагин **его нельзя отключить или удалить через «Менеджер плагинов»**

Значением обеих переменных является имя пакета плагина (поле `name` в `package.json`); несколько плагинов разделяются запятыми. В файле `.env` конфигурация выглядит следующим образом:

```bash
# Предустановка по умолчанию: плагин появляется в списке менеджера плагинов, но не активируется автоматически
APPEND_PRESET_LOCAL_PLUGINS=@my-project/plugin-hello,@my-project/plugin-hello-world

# Включение по умолчанию: автоматически устанавливается и активируется, отключить через интерфейс невозможно
APPEND_PRESET_BUILT_IN_PLUGINS=@my-project/plugin-hello,@my-project/plugin-hello-world
```

Как правило, для локальной разработки и отладки достаточно команды `nb plugin enable`, описанной выше. Эти две переменные больше подходят для сценариев «готового к использованию» дистрибутива — например, когда вы упаковываете приложение NocoBase с фиксированным набором плагинов и хотите, чтобы они были доступны сразу после инициализации.

:::tip Подсказка

- Плагин должен быть загружен локально и доступен для разрешения в каталоге `node_modules`. См. [Структура проекта плагина](./project-structure.md)
- После настройки переменных необходимо повторно выполнить `nocobase install` или `nocobase upgrade`, чтобы изменения вступили в силу
- Полное описание переменных среды см. в разделе [Переменные среды](../get-started/installation/env.md#append_preset_local_plugins)

:::

## Шаг 4: Сборка и упаковка

Когда вы будете готовы распространить плагин в другие среды, вам сначала потребуется его собрать и упаковать:

```bash
nb source build @my-project/plugin-hello --tar
# Или выполните в два этапа
nb source build @my-project/plugin-hello
nb source build @my-project/plugin-hello --tar
```

:::tip Подсказка

Если плагин был создан в репозитории исходного кода, первая сборка вызовет полную проверку типов всего репозитория, что может занять некоторое время. Рекомендуется убедиться, что зависимости установлены, а репозиторий находится в состоянии, пригодном для сборки.

:::

После завершения сборки упакованный файл по умолчанию находится в каталоге `source/storage/tar/`, команда выведет полный путь к tarball.

:::tip Подсказка

Перед публикацией плагина рекомендуется написать тесты для проверки основной логики. NocoBase предоставляет полный набор инструментов для серверного тестирования. Подробнее см. [Тестирование](./server/test.md).

:::

## Шаг 5: Загрузка в другое приложение NocoBase

Загрузите и распакуйте плагин в каталог `./storage/plugins` целевого приложения. Подробности см. в разделе [Установка и обновление плагинов](../get-started/install-upgrade-plugins.mdx).

## Связанные ссылки

- [Обзор разработки плагинов](./index.md) — микроядерная архитектура NocoBase и жизненный цикл плагина
- [Структура проекта плагина](./project-structure.md) — соглашения о структуре каталогов, пути загрузки и приоритеты плагинов
- [Обзор серверной разработки](./server/index.md) — общее введение в серверные плагины и основные концепции
- [Обзор клиентской разработки](./client/index.md) — общее введение в клиентские плагины и основные концепции
- [Сборка и упаковка](./build.md) — процесс сборки, упаковки и распространения плагина
- [Тестирование](./server/test.md) — написание тестов для серверных плагинов
- [Руководство по подключению AI Agent](../ai/quick-start.mdx) — установка NocoBase CLI и инициализация приложения
- [Установка приложения через CLI](../nocobase-cli/installation/cli.md) — полный процесс установки
- [Установка и обновление плагинов](../get-started/install-upgrade-plugins.mdx) — загрузка упакованного плагина в другие среды
- [Переменные среды](../get-started/installation/env.md) — настройка переменных среды для предустановленных и встроенных плагинов
