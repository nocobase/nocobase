---
title: "Обзор разработки плагинов NocoBase"
description: "Микроядерная архитектура NocoBase, жизненный цикл плагинов, структура каталогов, подключай и работай, полная интеграция клиента и сервера, исходный код client/server, метаинформация package.json."
keywords: "разработка плагинов,плагин NocoBase,микроядро,жизненный цикл плагинов,полная интеграция,расширение NocoBase"
---

# Обзор разработки плагинов

NocoBase использует **микроядерную архитектуру**, где ядро отвечает только за планирование жизненного цикла плагинов, управление зависимостями и инкапсуляцию базовых возможностей. Все бизнес-функции предоставляются в виде плагинов. Поэтому понимание структуры, жизненного цикла и способов управления плагинами — это первый шаг к кастомизации NocoBase.

## Ключевые концепции

- **Plug and Play (Подключай и работай)**: Вы можете устанавливать, включать или отключать плагины по мере необходимости, гибко комбинируя бизнес-функции без изменения кода.
- **Полная интеграция (Full-stack)**: Плагины обычно включают в себя как серверную, так и клиентскую реализацию, обеспечивая согласованность между логикой данных и взаимодействием с пользовательским интерфейсом.

## Базовая структура плагина

Каждый плагин представляет собой независимый npm-пакет и обычно имеет следующую структуру каталогов:

```bash
plugin-hello/
├─ package.json          # Название плагина, зависимости и метаинформация плагина NocoBase
├─ client-v2.js          # Результат сборки фронтенда для загрузки во время выполнения
├─ server.js             # Результат сборки бэкенда для загрузки во время выполнения
├─ src/
│  ├─ client-v2/         # Исходный код клиента, может регистрировать блоки, действия, поля и т.д.
│  └─ server/            # Исходный код сервера, может регистрировать ресурсы, события, команды и т.д.
```

## Предварительные требования

Перед разработкой плагинов необходимо инициализировать приложение через NocoBase CLI. CLI поддерживает два источника — npm и Git:

- **npm-источник** (`create-nocobase-app`): подходит для быстрого старта, готов к использованию сразу.
- **Git-источник** (рекомендуется): клонирование репозитория исходного кода NocoBase. При разработке с помощью AI можно напрямую обращаться к исходному коду ядра, что даёт лучшие результаты.

Подробнее см. [Установка через CLI](../nocobase-cli/installation/cli.md) или [Руководство по AI Agent](../ai/quick-start.mdx).

## Соглашения о каталогах и порядок загрузки

Приложение, созданное через `nb init`, имеет следующую структуру каталогов:

```bash
<app-path>/
├── .nb/                  # Метаданные CLI для текущего окружения
├── source/               # Исходный код приложения (проект NocoBase)
├── storage/              # Каталог данных времени выполнения
│   └── plugins/          # Скомпилированные плагины (загруженные или импортированные)
├── plugins/              # Исходный код ваших плагинов (nb scaffold plugin создаёт их здесь)
└── .env                  # Файл переменных окружения приложения
```

- `plugins/`: каталог исходного кода ваших плагинов. Плагины, созданные через `nb scaffold plugin`, размещаются здесь. `nb` автоматически синхронизирует их в `source/packages/plugins/` для процессов разработки и сборки — вам не нужно вручную работать с каталогом `source/`.
- `storage/plugins/`: здесь хранятся скомпилированные плагины, например, коммерческие версии или сторонние плагины.

## Жизненный цикл и состояния плагина

Плагин обычно проходит следующие этапы:

1.  **Создание (create)**: Создание шаблона плагина с помощью CLI.
2.  **Загрузка (pull)**: Загрузка пакета плагина локально, но он еще не записан в базу данных.
3.  **Включение (enable)**: При первом включении выполняется «регистрация + инициализация»; при последующих включениях загружается только логика.
4.  **Отключение (disable)**: Остановка работы плагина.
5.  **Удаление (remove)**: Полное удаление плагина из системы.

:::tip

- `pull` отвечает только за загрузку пакета плагина; фактический процесс установки запускается при первом `enable`.
- Если плагин только загружен (`pull`), но не включен, он не будет загружен.

:::

### Примеры команд CLI

```bash
# 1. Создание скелета плагина
nb scaffold plugin @my-project/plugin-hello

# 2. Включение плагина (при первом включении устанавливается автоматически)
nb plugin enable @my-project/plugin-hello

# 3. Отключение плагина
nb plugin disable @my-project/plugin-hello
```

## Интерфейс управления плагинами

Откройте менеджер плагинов в браузере, чтобы наглядно просматривать и управлять плагинами:

**Адрес по умолчанию:** [http://localhost:13000/admin/settings/plugin-manager](http://localhost:13000/admin/settings/plugin-manager)

![Менеджер плагинов](https://static-docs.nocobase.com/20251030195350.png)

## Связанные ссылки

- [Создание первого плагина](./write-your-first-plugin.md) — создайте блочный плагин с нуля и быстро освойте процесс разработки
- [Структура каталогов проекта](./project-structure.md) — соглашения о каталогах проекта NocoBase и порядок загрузки плагинов
- [Обзор серверной разработки](./server/index.md) — общее введение и ключевые концепции серверных плагинов
- [Обзор клиентской разработки](./client/index.md) — общее введение и ключевые концепции клиентских плагинов
- [Сборка и упаковка](./build.md) — процесс сборки и упаковки плагинов
- [Управление зависимостями](./dependency-management.md) — объявление и управление зависимостями плагинов
