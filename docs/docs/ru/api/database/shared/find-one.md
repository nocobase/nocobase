**Тип**

```typescript
type FindOneOptions = Omit<FindOptions, 'limit'>;
```

**Параметры**

Большинство параметров такие же, как у `find()`. Разница в том, что `findOne()` возвращает только одну запись, поэтому параметр `limit` не требуется, и во время запроса он всегда равен `1`.