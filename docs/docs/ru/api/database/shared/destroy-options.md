:::tip Уведомление о переводе ИИ
Эта документация была автоматически переведена ИИ.
:::

**Тип**

```typescript
interface DestroyOptions extends SequelizeDestroyOptions {
  filter?: Filter;
  filterByTk?: TargetKey | TargetKey[];
  truncate?: boolean;
  context?: any;
}
```

**Подробности**

- `filter`: Определяет условия фильтрации для удаляемых записей. Подробнее об использовании `Filter` вы можете узнать в методе [`find()`](#find).
- `filterByTk`: Определяет условия фильтрации для удаляемых записей по `TargetKey`.
- `truncate`: Указывает, следует ли очистить данные коллекции. Этот параметр действует только в том случае, если не переданы параметры `filter` или `filterByTk`.
- `transaction`: Объект транзакции. Если параметр транзакции не передан, метод автоматически создаст внутреннюю транзакцию.