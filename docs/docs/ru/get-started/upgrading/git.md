:::tip Уведомление о переводе ИИ
Эта документация была автоматически переведена ИИ.
:::

# Обновление NocoBase, установленного из исходного кода Git

:::warning Подготовка перед обновлением

- Обязательно сделайте резервную копию базы данных.
- Остановите запущенный экземпляр NocoBase (`Ctrl + C`).

:::

## 1. Перейдите в каталог проекта NocoBase

```bash
cd my-nocobase-app
```

## 2. Получите последнюю версию кода

```bash
git pull
```

## 3. Удалите кэш и старые зависимости (необязательно)

Если обычный процесс обновления завершится неудачей, вы можете попробовать очистить кэш и зависимости, а затем загрузить их заново.

```bash
# Очистка кэша NocoBase
yarn nocobase clean
# Удаление зависимостей
yarn rimraf -rf node_modules # эквивалентно rm -rf node_modules
```

## 4. Обновите зависимости

📢 Из-за таких факторов, как сетевое окружение и конфигурация системы, этот шаг может занять более десяти минут.

```bash
yarn install
```

## 5. Выполните команду обновления

```bash
yarn nocobase upgrade
```

## 6. Запустите NocoBase

```bash
yarn dev
```

:::tip Совет для производственной среды

Не рекомендуется развертывать NocoBase, установленный из исходного кода, непосредственно в производственной среде (информацию о развертывании в производственной среде см. в разделе [Развертывание в производственной среде](../deployment/production.md)).

:::

## 7. Обновление сторонних плагинов

См. [Установка и обновление плагинов](../install-upgrade-plugins.mdx)