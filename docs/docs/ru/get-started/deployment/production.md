# Развертывание в производственной среде

При развертывании NocoBase в производственной среде установка зависимостей может быть довольно сложной из-за различий в методах сборки для разных систем и окружений. Для получения полного функционала мы рекомендуем использовать **Docker** для развертывания. Если ваша системная среда не позволяет использовать Docker, вы также можете развернуть NocoBase с помощью **create-nocobase-app**.

:::warning Внимание

Не рекомендуется развертывать NocoBase напрямую из исходного кода в производственной среде. Исходный код имеет множество зависимостей, он объемен, а полная компиляция требует значительных ресурсов CPU и памяти. Если вам все же необходимо развернуть NocoBase из исходного кода, рекомендуется сначала собрать собственный образ Docker, а затем выполнить развертывание.

:::

:::warning Внимание

Если вы развёртываете несколько независимых сервисов NocoBase, используйте отдельный `hostname` для каждого сервиса, например разные поддомены. Не различайте сервисы только по порту, как в `https://example.com:13000` и `https://example.com:14000`.

NocoBase использует cookies для сохранения состояния входа и [прав доступа к файлам](../../file-manager/stable-url.md). Браузеры не изолируют cookies по портам, поэтому сервисы на разных портах под одним `hostname` могут использовать одноимённые cookies. Это может привести к перезаписи состояния входа или ошибкам авторизации при предварительном просмотре и скачивании файлов.

Подприложения в одном развёртывании NocoBase не подпадают под это ограничение. Cookies входа различаются по имени приложения, поэтому основное приложение и подприложения с разными именами могут использовать один `hostname`.

Однако независимые сервисы по-прежнему необходимо изолировать. Если другой сервис NocoBase работает на другом порту под тем же `hostname` и содержит основное или дочернее приложение с тем же именем, cookies всё равно могут конфликтовать.

Используйте адреса наподобие `app1.example.com` и `app2.example.com`, а затем направляйте их к разным сервисам NocoBase через Nginx или Caddy.

:::

## Процесс развертывания

Для развертывания в производственной среде вы можете обратиться к существующим шагам по установке и обновлению.

### Новая установка

- [Установка Docker](../installation/docker.mdx)
- [Установка create-nocobase-app](../installation/create-nocobase-app.mdx)

### Обновление приложения

- [Обновление установки Docker](../installation/docker.mdx)
- [Обновление установки create-nocobase-app](../installation/create-nocobase-app.mdx)

### Установка и обновление сторонних плагинов

- [Установка и обновление плагинов](../install-upgrade-plugins.mdx)

## Проксирование статических ресурсов

В производственной среде рекомендуется управлять статическими ресурсами с помощью прокси-сервера, например:

- [nginx](./static-resource-proxy/nginx.md) 
- [caddy](./static-resource-proxy/caddy.md)
- [cdn](./static-resource-proxy/cdn.md)

## Часто используемые команды для эксплуатации

В зависимости от способа установки вы можете использовать следующие команды для управления процессами NocoBase:

- [docker compose](./common-commands/docker-compose.md)
- [pm2](./common-commands/pm2.md)
