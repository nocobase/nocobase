---
title: "作成日"
description: "作成日フィールドは、レコードの作成日時を自動的に記録します。"
keywords: "作成日,createdAt,システムフィールド,NocoBase"
---

# 作成日

## はじめに

NocoBase では、**作成日（Created at）**を使用して、レコードの作成日時を自動的に記録できます。

作成日フィールドは通常、プリセットフィールドとして生成されます。並べ替え、絞り込み、監査、ワークフローの条件、データ統計などに適しています。

契約日や有効期限など、業務上の日付を手動で入力する必要がある場合は、[日付](../datetime/date.md)または[日時](../datetime/datetime.md)を使用してください。

## 利用シーン

作成日フィールドは、次のような業務シーンに適しています。

- 作成日時で並べ替える
- 特定の期間に作成されたデータを絞り込む
- レコードの作成日時を監査する
- 新規レコードの作成日時をワークフローで判定する

## 作成時の設定

データテーブルの「Configure fields」ページで「Add field」をクリックし、「作成日」を選択すると、作成日フィールドを作成できます。

![20240512174347](https://static-docs.nocobase.com/20240512174347.png)

| 設定 | 説明 |
| --- | --- |
| Field interface | フィールドのインターフェースタイプ。作成日は `createdAt` に対応し、画面上での入力方法や表示方法を決定します。 |
| Field display name | 画面上に表示されるフィールド名です。たとえば「作成日」「作成日時」などです。業務担当者が直接理解できる名称を使用することを推奨します。 |
| Field name | API、リレーションフィールド、権限、ワークフローなどで内部参照するためのフィールド識別名です。作成後は通常変更せず、英字、数字、アンダースコアのみ使用でき、先頭は英字でなければなりません。 |
| Field type | データ層におけるフィールドのタイプです。作成日には通常 `date` を使用します。 |
| Default value | デフォルト値です。レコードの新規作成時にユーザーが入力しなかった場合、自動的に値を設定できます。 |
| Validation rules | システムによって自動的に管理されるため、通常は手動で検証する必要はありません。 |
| Description | フィールドの説明です。フィールドの意味、入力要件、データソース、管理者などを記載するのに適しています。 |

:::warning 注意

フィールド名は、作成後にページブロック、権限、ワークフロー、API から参照されます。後から変更すると設定の調整コストが発生するため、作成前に命名を確認してください。

:::

## フィールドの特性

作成日フィールドのデフォルトの動作は次のとおりです。

| 特性 | 説明 |
| --- | --- |
| デフォルトの Field interface | `createdAt`。 |
| デフォルトの Field type | `date`。 |
| 選択可能な Field type | `date`。 |
| ページコンポーネント | システムが自動的に入力し、画面上では通常読み取り専用で表示されます。 |
| 絞り込み | 時刻および期間による絞り込みに対応しています。 |
| 並べ替え | 時刻による並べ替えに対応しています。 |
| 検証 | システムが自動的に入力します。 |

## 設定の編集

作成後、フィールド右側の「Edit」をクリックすると、作成日フィールドの設定を編集できます。フィールドの編集は、主に NocoBase でのフィールドの表示方法や使用方法を調整するために行います。たとえば、表示名、説明、デフォルト値、検証ルール、フィールド固有の設定などを変更できます。

フィールドがメインデータベースですでに同期されているテーブルに由来する場合、編集時には通常、フィールドマッピングを行います。つまり、データベースのフィールドを NocoBase の Field type と Field interface にマッピングします。

| 設定 | 編集可否 | 説明 |
| --- | --- | --- |
| Field display name | はい | 画面上に表示されるフィールド名を変更します。フィールド識別名は変更されません。 |
| Field name | いいえ | フィールド識別名は、作成後通常、編集フォームでは変更できません。 |
| Field interface | 条件付きで可 | メインデータベースのフィールドや同期フィールドは、フィールドマッピング時に調整できます。変更すると、画面上の入力方法、表示方法、検証方法に影響します。 |
| Field type | 条件付きで可 | メインデータベースのフィールドや同期フィールドは、フィールドマッピング時に調整できます。変更前に、既存のデータを新しいタイプで使用できるか確認する必要があります。 |
| Default value | はい | レコードの新規作成時に使用するデフォルト値を調整します。 |
| Validation rules | はい | フィールドの検証ルールを調整します。 |
| Description | はい | フィールドの意味、入力要件、データソース、管理者などを補足します。 |

:::warning 注意

Field type や Field interface の切り替えは、単に表示名を変更することとは異なります。フィールドの保存方法、入力コンポーネント、検証ルール、絞り込み条件、ワークフロー変数の使用方法に影響します。既存データが多い場合は、データ形式が適合するか事前に確認してください。

:::

## フィールドの削除

フィールド右側の「Delete」をクリックすると、作成日フィールドを削除できます。メインデータベースでは、複数のフィールドを選択して一括削除することもできます。

メインデータベースで新しく作成した作成日フィールドを削除すると、通常、データベース内の実際の列と、その列に保存されている既存データも同時に削除されます。データベースから同期したフィールドや外部データソースからマッピングしたフィールドを削除する場合の影響範囲は、対応するデータソースとフィールドの由来によって異なります。

:::danger 警告

フィールドを削除すると、ページブロック、フォーム、絞り込み、権限、ワークフロー、API、インポート・エクスポート、既存データに影響する可能性があります。削除前に、そのフィールドが業務設定で参照されていないか確認してください。

:::

## ページ設定での使用

作成日フィールドは、一覧、詳細、絞り込み、監査での使用に適しています。
![20260710153223](https://static-docs.nocobase.com/20260710153223.png)

| シーン | 用途 |
| --- | --- |
| テーブルブロック | 作成日時を表示し、並べ替えます。 |
| 絞り込みブロック | 特定の期間に作成されたレコードを絞り込みます。 |
| 詳細ブロック | レコードの作成日時を確認します。 |
| ワークフロー | 時間条件として判定に使用します。 |

## 関連リンク

- [フィールド](../index.md) — フィールドの役割、分類、マッピングロジックについて説明します
- [通常テーブル](../../../data-source-main/general-collection.md) — 通常テーブルでのフィールドの作成と管理
- [日時（タイムゾーンを含む）](../datetime/datetime.md) — 業務上の日時を保存します
- [更新日](./updated-at.md) — 更新日時を自動的に記録します
