---
pkg: "@nocobase/plugin-calendar"
title: "カレンダーテーブル"
description: "カレンダーテーブルは、会議、スケジュール、授業、当番など、時間範囲を持つデータを保存し、カレンダーブロックと組み合わせてイベント記録を表示・編集するために使用します。"
keywords: "カレンダーテーブル,Calendar Collection,カレンダーイベント,繰り返しイベント,カレンダーブロック,NocoBase"
---

# カレンダーテーブル

## 概要

カレンダーテーブルは、会議室の予約、プロジェクトのスケジュール、授業計画、当番計画、イベント予定など、時間範囲を持つデータの保存に適しています。本質的には通常のデータテーブルですが、カレンダーイベントに関連するフィールドがあらかじめ用意されているため、後からカレンダーブロックと組み合わせて使用できます。

カレンダーテーブルは、メインデータベースのページからのみ作成できます。外部データベース、REST APIデータソース、外部NocoBaseデータソースでは、カレンダーテーブルを作成できません。

## 適用シーン

カレンダーテーブルは、次のような業務シーンに適しています。

- 会議室、車両、設備の予約
- プロジェクトのスケジュール、タスク計画、マイルストーンの設定
- 時間割、研修計画、イベント予定
- 当番計画、シフト記録、巡回点検計画
- 日、週、月単位で確認する必要があるイベント記録

## 作成設定

メインデータベースで「Create collection」をクリックし、「Calendar collection」を選択すると、カレンダーテーブルを作成できます。

カレンダーテーブルの作成設定は、通常のテーブルと基本的に同じです。`Preset fields` は一般的なシステムフィールドを制御するために使用され、カレンダーテーブルには繰り返しイベントを保存するためのフィールドもあらかじめ用意されます。

| 設定 | 説明 |
| --- | --- |
| Collection display name | データテーブルを画面上で表示する名前です。例：「会議室予約」「授業計画」「当番計画」。 |
| Collection name | データテーブルを識別する名前です。API、リレーションフィールド、権限、ワークフローなどで内部参照する際に使用します。 |
| Inherits | 継承する親テーブルを選択します。メインデータベースがPostgreSQLの場合のみ表示されます。 |
| Categories | データテーブルの分類です。分類はデータテーブル管理画面での整理方法にのみ影響し、データテーブルの構造は変更しません。 |
| Description | データテーブルの説明です。このカレンダーテーブルにどのようなイベントを保存するか、誰が管理するか、どの業務プロセスに関連するかなどを記載できます。 |
| Preset fields | 事前設定フィールドです。カレンダーテーブルを作成する際は、システムフィールドとカレンダーテーブルの組み込みフィールドを残しておくことをおすすめします。 |

### 組み込みフィールド

カレンダーテーブルの作成後、通常は次の組み込みフィールドが含まれます。`cron` と `exclude` は、繰り返しイベントのルールと除外日を保存するために使用されます。

| フィールド | フィールド名 | 説明 |
| --- | --- | --- |
| ID | `id` | イベント記録を一意に識別するための、デフォルトの主キーです。 |
| 作成日時 | `createdAt` | イベント記録の作成日時を自動的に記録します。 |
| 作成者 | `createdBy` | イベント記録を作成したユーザーを自動的に記録します。 |
| 更新日時 | `updatedAt` | イベント記録が最後に更新された日時を自動的に記録します。 |
| 更新者 | `updatedBy` | イベント記録を最後に更新したユーザーを自動的に記録します。 |
| 並び順 | `sort` | イベント記録の並び順の値を保存し、ドラッグ＆ドロップによる並べ替えなどをサポートします。 |
| Repeats | `cron` | 毎日、毎週、毎月、毎年などの繰り返しイベントのルールを保存します。 |
| Exclude | `exclude` | 繰り返しイベントから除外する日付を保存します。通常はカレンダー上の操作によって自動的に管理されます。 |
| スペース | `space` | [マルチスペースプラグイン](../../multi-app/multi-space/index.md)を有効にすると使用でき、スペース単位でデータを分離します。マルチスペースを有効にしていない場合は表示されません。 |

カレンダーブロックでカレンダーテーブルを使用する場合は、イベントの表示に使用する業務フィールドも指定する必要があります。

| 設定 | 説明 |
| --- | --- |
| タイトルフィールド | カレンダー上でのイベントのタイトルを指定します。例：「会議のテーマ」「授業名」。 |
| 開始日フィールド | イベントの開始日時を指定します。通常は日時フィールドを使用します。 |
| 終了日フィールド | イベントの終了日時を指定します。通常は日時フィールドを使用します。 |

:::warning 注意

`cron` と `exclude` は通常、カレンダー機能によって管理されるため、通常の業務フィールドとして直接編集することはおすすめしません。タイトル、開始日、終了日の各フィールドは、業務要件に応じて作成・設定する必要があります。設定しない場合、カレンダーブロックでイベントを正しく表示できません。

:::

### 主キー

カレンダーテーブルにも、通常のテーブルと同様に主キーが必要です。テーブルを作成する際は、IDの事前設定フィールドを残しておくことをおすすめします。デフォルトの主キータイプは `Snowflake ID (53-bit)` です。

カレンダーテーブルに主キーがない場合は、データテーブルの編集時に「Record unique key」を設定してください。設定しないと、カレンダーブロックでイベント記録を正しく開く、編集する、または特定することができない場合があります。

## 編集設定

データテーブル一覧で、カレンダーテーブルの右側にある「Edit」をクリックすると、データテーブルの表示名、分類、説明、簡易ページネーションモード、「Record unique key」などの設定を変更できます。

カレンダーテーブルの `cron`、`exclude` などの組み込みフィールドは、通常カレンダー機能に使用されるため、別の業務上の意味に変更することはおすすめしません。イベント情報を拡張する必要がある場合は、場所、参加者、会議室、ステータスなどの通常の業務フィールドを追加してください。

## データテーブルの削除

データテーブル一覧で、カレンダーテーブルの右側にある「Delete」をクリックすると、カレンダーテーブルを削除できます。

カレンダーテーブルを削除すると、イベント記録、カレンダー組み込みフィールドのデータ、および関連するCollectionメタデータが削除されます。削除する前に、カレンダーブロック、テーブルブロック、権限、ワークフロー、APIが引き続きこのテーブルに依存していないか確認してください。

:::danger 警告

カレンダーテーブルには通常、スケジュール、予約、当番などの時間データが保存されています。削除すると、過去のイベントや繰り返しルールもすべて失われます。操作する前に、データがバックアップ済みであるか、または不要になっていることを確認してください。

:::

## ページ設定での使用

カレンダーテーブルでは、[通常のテーブル](../data-source-main/general-collection.md)の大半のデータブロックを使用して、データの追加、削除、変更、検索を行えます。また、通常はカレンダーブロックと組み合わせて使用します。

| ブロック | 用途 |
| --- | --- |
| [カレンダーブロック](../../interface-builder/blocks/data-blocks/calendar.md) | 日、週、月などのビューでイベント記録を表示し、カレンダー上でイベントを作成、表示、編集します。 |
| [テーブルブロック](../../interface-builder/blocks/data-blocks/table.md) | リスト形式でイベント記録を表示、絞り込み、一括管理します。 |
| [フォームブロック](../../interface-builder/blocks/data-blocks/form.md) | 1件のイベント記録を追加または編集します。 |
| [詳細ブロック](../../interface-builder/blocks/data-blocks/details.md) | 1件のイベントの詳細情報を表示します。 |

## 関連リンク

- [通常のテーブル](../data-source-main/general-collection.md) — 共通設定とブロックの使用方法を確認
- [日付と時刻フィールド](../data-modeling/collection-fields/datetime/datetime.md) — イベントの開始日時と終了日時のフィールドを作成
- [カレンダーブロック](../../interface-builder/blocks/data-blocks/calendar.md) — ページ上でデータをカレンダー形式で表示
- [マルチスペース](../../multi-app/multi-space/index.md) — スペースフィールドとスペース分離機能について確認