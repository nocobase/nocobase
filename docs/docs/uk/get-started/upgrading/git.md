:::tip Повідомлення про переклад ШІ
Ця документація була автоматично перекладена штучним інтелектом.
:::


# Оновлення встановлення з вихідного коду Git

:::warning Підготовка перед оновленням

- Обов'язково зробіть резервну копію бази даних
- Зупиніть NocoBase, що працює (`Ctrl + C`)

:::

## 1. Перейдіть до каталогу проєкту NocoBase

```bash
cd my-nocobase-app
```

## 2. Завантажте останній код

```bash
git pull
```

## 3. Видаліть кеш та старі залежності (необов'язково)

Якщо звичайний процес оновлення не вдається, спробуйте очистити кеш та залежності, а потім завантажити їх знову.

```bash
# Очистити кеш nocobase
yarn nocobase clean
# Видалити залежності
yarn rimraf -rf node_modules # еквівалентно rm -rf node_modules
```

## 4. Оновіть залежності

📢 Через такі фактори, як мережеве середовище та конфігурація системи, цей крок може зайняти понад десять хвилин.

```bash
yarn install
```

## 5. Виконайте команду оновлення

```bash
yarn nocobase upgrade
```

## 6. Запустіть NocoBase

```bash
yarn dev
```

:::tip Порада щодо виробничого середовища

Не рекомендується розгортати NocoBase, встановлений з вихідного коду, безпосередньо у виробничому середовищі (для виробничих середовищ, будь ласка, зверніться до [Розгортання у виробничому середовищі](../deployment/production.md)).

:::

## 7. Оновлення сторонніх плагінів

Зверніться до [Встановлення та оновлення плагінів](../install-upgrade-plugins.mdx)