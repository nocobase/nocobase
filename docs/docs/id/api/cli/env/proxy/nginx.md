# nb env proxy nginx

`nb env proxy nginx` menghasilkan konfigurasi Nginx untuk env yang dikelola CLI. Gunakan jika Anda sudah memakai Nginx untuk mengelola situs atau masih ingin menangani sertifikat, cache, dan kontrol akses sendiri.
