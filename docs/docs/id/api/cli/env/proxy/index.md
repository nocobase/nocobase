# nb env proxy

Di NocoBase CLI, `nb env proxy` sekarang adalah sebuah topik. Perintah ini tidak langsung menghasilkan konfigurasi. Gunakan perintah ini untuk melihat subperintah `nb env proxy nginx` dan `nb env proxy caddy`.
