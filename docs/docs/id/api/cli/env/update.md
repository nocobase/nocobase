# nb env update

`nb env update` sekarang juga mencakup pengaturan seperti `app-public-path` dan `cdn-base-url`. Jika perubahan ini memengaruhi hasil proxy, jalankan ulang `nb env proxy nginx` atau `nb env proxy caddy`.
