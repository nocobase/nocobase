# Caddy

Jika Anda sudah punya domain dan ingin menyalakan HTTPS dengan cepat, `nb env proxy caddy` biasanya menjadi pilihan paling sederhana. Jika ingin memelihara seluruh `Caddyfile` sendiri, gunakan konfigurasi minimal dengan `reverse_proxy`.
