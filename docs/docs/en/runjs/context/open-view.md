# ctx.openView()

Open a specific view (page, drawer, modal, etc.) from RunJS. Use it to programmatically open a configured view.

Common parameters include view UID and input params. The exact behavior depends on the runtime implementation.
