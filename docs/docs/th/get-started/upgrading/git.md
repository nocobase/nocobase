:::tip
เอกสารนี้แปลโดย AI หากมีข้อมูลที่ไม่ถูกต้อง โปรดดู[เวอร์ชันภาษาอังกฤษ](/en)
:::


# การอัปเกรด NocoBase ที่ติดตั้งจาก Git Source

:::warning การเตรียมตัวก่อนอัปเกรด

- อย่าลืมสำรองข้อมูลฐานข้อมูลของคุณก่อนเสมอ
- หยุดการทำงานของ NocoBase ที่กำลังรันอยู่ (`Ctrl + C`)

:::

## 1. เปลี่ยนไปยังไดเรกทอรีโปรเจกต์ NocoBase

```bash
cd my-nocobase-app
```

## 2. ดึงโค้ดล่าสุด

```bash
git pull
```

## 3. ลบแคชและ Dependencies เก่า (ไม่บังคับ)

หากขั้นตอนการอัปเกรดปกติล้มเหลว คุณสามารถลองล้างแคชและ Dependencies แล้วดาวน์โหลดใหม่ได้ครับ/ค่ะ

```bash
# ล้างแคชของ nocobase
yarn nocobase clean
# ลบ Dependencies
yarn rimraf -rf node_modules # เทียบเท่ากับ rm -rf node_modules
```

## 4. อัปเดต Dependencies

📢 เนื่องจากปัจจัยต่างๆ เช่น สภาพแวดล้อมเครือข่ายและการกำหนดค่าระบบ ขั้นตอนนี้อาจใช้เวลามากกว่าสิบนาทีครับ/ค่ะ

```bash
yarn install
```

## 5. รันคำสั่งอัปเกรด

```bash
yarn nocobase upgrade
```

## 6. เริ่มต้น NocoBase

```bash
yarn dev
```

:::tip เคล็ดลับสำหรับ Production Environment

ไม่แนะนำให้ติดตั้ง NocoBase จาก Git Source โดยตรงใน Production Environment นะครับ/คะ (สำหรับ Production Environment โปรดดู [การติดตั้งใน Production Environment](../deployment/production.md))

:::

## 7. การอัปเกรดปลั๊กอินจากภายนอก

โปรดดู [การติดตั้งและอัปเกรดปลั๊กอิน](../install-upgrade-plugins.mdx)