# ctx.i18n

i18next 实例。

## 类型定义

```ts
interface i18n: {
  language: string;
  t: (key: string, options?: any) => string;
};
```

- `language`：当前激活的语言代码（如 `zh-CN`、`en-US`）
- `t(key, options?)`：翻译函数，与应用的 `i18n.t` 等价
