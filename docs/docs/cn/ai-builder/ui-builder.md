---
title: "界面配置"
description: "界面配置 Skill 用于创建和编辑 NocoBase 的页面、区块、字段和操作配置。"
keywords: "AI 搭建,界面配置,页面,区块,弹窗,联动,UI Builder"
---

# 界面配置

## 简介

界面配置 Skill 用于创建和编辑 NocoBase 的页面、区块、字段和操作配置——你用业务语言描述想要的页面，它来处理蓝图生成、区块布局和交互联动。

## 安装

```bash
npx skills add nocobase/skills --skill nocobase-ui-builder -y
```

## 能力范围

可以做：

- 创建完整页面：表格、筛选表单、详情弹窗一步到位
- 编辑已有页面：添加区块、调整字段、配置弹窗
- 设置交互联动：默认值、字段显隐、计算联动、操作按钮状态
- 使用模板复用：重复的弹窗和区块可以保存为模板
- 支持多页面任务：按顺序逐页搭建

不能做：

- 不能配置 ACL 权限（用[权限配置 Skill](./acl)）
- 不能设计数据表结构（用[数据建模 Skill](./data-modeling)）
- 不能编排工作流（用[工作流管理 Skill](./workflow)）
- 不能处理非现代页面（v2）的导航

## 提示词示例

### 场景 A：创建管理页面

```
帮我创建一个客户管理页面，包含姓名搜索框和客户表格，表格显示名称、电话、邮箱、创建时间
```

Skill 会先读取集合字段，生成页面蓝图并输出 ASCII 预览，确认后写入。

### 场景 B：配置弹窗

```
点击表格中的客户名称时弹出详情页，显示所有字段
```

会优先用字段弹窗（点击即弹出），而不是额外添加操作按钮。

### 场景 C：设置联动规则

```
当状态字段选择「已完成」时，自动隐藏「处理人」字段
```

会通过交互联动规则实现，不需要手动编写配置。

## 常见问题

**页面创建后区块里没有数据怎么办？**

先确认对应的数据表里确实有记录。另外检查区块绑定的集合和数据源是否正确。

**弹窗里想放多个区块怎么办？**

可以在提示词中描述弹窗内容，比如「编辑弹窗里放一个表单和一个关联表格」。Skill 会生成包含多个区块的自定义弹窗布局。

## 相关链接

- [AI 搭建概述](./index.md) — 所有 AI 搭建 Skill 的总览和安装方式
- [数据建模](./data-modeling) — 用 AI 创建和管理数据表、字段、关联关系
- [权限配置](./acl) — 配置角色和数据访问权限
- [工作流管理](./workflow) — 创建、编辑和诊断工作流
