# NocoBase Source Index

This directory records source evidence for API-backed CLI commands.

The CLI uses `swagger:get` as the runtime command source. When a command is added, record the owning package and endpoint mapping in `command-to-api-map.json`.
