name: NocoBase frontEnd test

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - main
      - next
      - develop
    paths:
      - 'packages/**'
      - '**/yarn.lock'
      - '.github/workflows/nocobase-test-frontend.yml'
      - '!packages/core/acl/**'
      - '!packages/core/actions/**'
      - '!packages/core/database/**'
      - '!packages/core/server/**'
      - '!packages/plugins/**/src/server/**'
  pull_request:
    paths:
      - '.github/workflows/nocobase-test-frontend.yml'
      - 'packages/**'
      - '**/yarn.lock'
      - '!packages/core/acl/**'
      - '!packages/core/actions/**'
      - '!packages/core/database/**'
      - '!packages/core/server/**'
      - '!packages/plugins/**/src/server/**'

jobs:
  frontend-test:
    strategy:
      fail-fast: false
      matrix:
        node_version: ['22']
        shard: [1, 2, 3, 4, 5, 6, 7, 8]
    runs-on: ubuntu-latest
    container: node:${{ matrix.node_version }}
    steps:
      - uses: actions/checkout@v2

      - name: Use Node.js ${{ matrix.node_version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node_version }}
          cache: 'yarn'

      - run: yarn install
      - run: yarn test:client --pool=forks --shard=${{ matrix.shard }}/8
    # Vitest shards by hashed file path and file count, not historical runtime; use more shards to reduce long-tail UI test imbalance.
    timeout-minutes: 60
