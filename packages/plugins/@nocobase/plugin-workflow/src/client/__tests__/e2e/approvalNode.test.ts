import { expect, test } from '@nocobase/test/client';
import { dayjs } from '@nocobase/utils';
import { CreateWorkFlow } from './pageobject/workFlow';

test.describe('approver', () => {});

test.describe('Display approval form data', () => {});

test.describe('Operational Flow of Approval', () => {});

test.describe('Approval of initiating blocks', () => {});

test.describe('Approval Processing Block', () => {});
