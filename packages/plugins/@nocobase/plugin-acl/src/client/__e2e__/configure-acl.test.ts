import { expect, test } from '@nocobase/test/client';

test.describe('allows to configure interface', () => {
  test('view ', async ({ page, mockPage }) => {
    await mockPage().goto();
  });
});

test.describe('allows to install ,install,disabled plugins', () => {
  test('view ', async ({ page, mockPage }) => {
    await mockPage().goto();
  });
});

test.describe('allows to confgiure plugins', () => {
  test('view ', async ({ page, mockPage }) => {
    await mockPage().goto();
  });
});

test.describe('allows to clear cache,reboot application', () => {
  test('view ', async ({ page, mockPage }) => {
    await mockPage().goto();
  });
});

test.describe('new menu items allow to be asscessed by default', () => {
  test('view ', async ({ page, mockPage }) => {
    await mockPage().goto();
  });
});
