# ctx.collectionField

Reference to the field metadata when running inside field models.

## Examples

See `@nocobase/plugin-flow-engine/context/collection-field/examples.md` for snippets such as:

- Inspect Field
