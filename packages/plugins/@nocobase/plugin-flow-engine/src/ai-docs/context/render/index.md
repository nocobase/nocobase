# ctx.render()

Render a React element/HTML string/DOM node into the current container.

## Examples

See `@nocobase/plugin-flow-engine/context/render/examples.md` for snippets such as:

- Render React Node
