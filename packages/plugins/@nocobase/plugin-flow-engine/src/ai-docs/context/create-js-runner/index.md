# ctx.createJSRunner()

Instantiate a JSRunner that executes sandboxed JavaScript with optional globals/timeouts.

## Examples

See `@nocobase/plugin-flow-engine/context/create-js-runner/examples.md` for snippets such as:

- Create Runner
