# ctx.modal

`ctx.modal` mirrors the FlowEngine dialog helpers. It exposes `confirm`, `info`, `success`, `error`, etc., and returns Promises that resolve when the user closes the modal.

## Example

- `@nocobase/plugin-flow-engine/context/modal/examples.md` demonstrates `ctx.modal.confirm/info` for destructive actions and status displays.
