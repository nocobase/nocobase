---
title: "Detach Delegate"
description: "Call this when the delegate context is no longer needed (for example when a child block unmounts)."
---

# Examples

## Detach Delegate

Call this when the delegate context is no longer needed (for example when a child block unmounts).

```ts
ctx.removeDelegate(delegateCtx);
```
