# ctx.removeDelegate()

Detach a previously added delegate context so property lookups stop falling back to it.

## Examples

See `@nocobase/plugin-flow-engine/context/remove-delegate/examples.md` for snippets such as:

- Detach Delegate
