# ctx.clearDelegates()

Remove all delegate contexts, leaving only the base FlowContext on the lookup chain.

## Examples

See `@nocobase/plugin-flow-engine/context/clear-delegates/examples.md` for snippets such as:

- Reset Delegate Chain
