# ctx.aclCheck()

Convenience wrapper around ctx.acl.can that throws when access is denied.

## Examples

See `@nocobase/plugin-flow-engine/context/acl-check/examples.md` for snippets such as:

- Ensure Access
