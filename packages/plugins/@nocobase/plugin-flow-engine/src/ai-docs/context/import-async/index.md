# ctx.importAsync()

Load an ES module dynamically (works in dev/prod). Returns the module namespace once resolved.

## Examples

See `@nocobase/plugin-flow-engine/context/import-async/examples.md` for snippets such as:

- Import Module
