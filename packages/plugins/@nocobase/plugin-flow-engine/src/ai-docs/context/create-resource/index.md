# ctx.createResource()

Instantiate a data resource (MultiRecordResource, SingleRecordResource, SQLResource, etc.) on demand.

## Examples

See `@nocobase/plugin-flow-engine/context/create-resource/examples.md` for snippets such as:

- Create Multi Record Resource
