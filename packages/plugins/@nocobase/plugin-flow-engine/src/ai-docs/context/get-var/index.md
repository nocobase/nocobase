# ctx.getVar()

Read a variable exposed to the flow runtime (e.g., from workflow inputs or upstream steps).

## Examples

See `@nocobase/plugin-flow-engine/context/get-var/examples.md` for snippets such as:

- Read Variable
