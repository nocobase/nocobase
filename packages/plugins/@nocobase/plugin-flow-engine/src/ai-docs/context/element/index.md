# ctx.element

ElementProxy pointing to the sandboxed container for JS blocks/fields/items.

## Examples

See `@nocobase/plugin-flow-engine/context/element/examples.md` for snippets such as:

- Write HTML
