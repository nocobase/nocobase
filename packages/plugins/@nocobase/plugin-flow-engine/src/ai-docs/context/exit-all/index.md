# ctx.exitAll()

Abort the entire flow, skipping the remaining steps and nested flows.

## Examples

See `@nocobase/plugin-flow-engine/context/exit-all/examples.md` for snippets such as:

- Abort Entire Flow
