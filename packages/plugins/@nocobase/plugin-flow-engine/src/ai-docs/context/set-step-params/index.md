# ctx.setStepParams()

Persist parameters for a given step so later executions reuse them.

## Examples

See `@nocobase/plugin-flow-engine/context/set-step-params/examples.md` for snippets such as:

- Persist Params
