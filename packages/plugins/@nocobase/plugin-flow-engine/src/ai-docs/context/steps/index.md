# ctx.steps

Internal map of flow steps (mostly useful when writing advanced step handlers).

## Examples

See `@nocobase/plugin-flow-engine/context/steps/examples.md` for snippets such as:

- Inspect Step Keys
