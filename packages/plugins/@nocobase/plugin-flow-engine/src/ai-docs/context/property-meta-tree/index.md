# ctx.getPropertyMetaTree()

Inspect metadata for every property registered via ctx.defineProperty so AI tools can display types and descriptions.

## Examples

See `@nocobase/plugin-flow-engine/context/property-meta-tree/examples.md` for snippets such as:

- Read Meta Tree
