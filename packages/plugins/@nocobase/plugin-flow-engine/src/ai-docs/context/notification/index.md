# ctx.notification

`ctx.notification` is the Ant Design notification instance bound to the runtime. Use it for persistent toasts that stay visible until dismissed.

## Example

- `@nocobase/plugin-flow-engine/context/notification/examples.md` invokes `ctx.notification.success/error` to reflect flow outcomes.
