# Examples

## Notify Success

Use this snippet to notify success.

```ts
ctx.notification.success({
  message: 'Success',
  description: message,
});
```

## Notify Error

Use this snippet to notify error.

```ts
ctx.notification.error({
  message: 'Error',
  description: message,
});
```
