---
title: "Translate Simple Key"
description: "Use ctx.t() with a simple key or with interpolation"
---

# Translate Simple Key

Use `ctx.t()` with a simple key or with `{{var}}` interpolation.

```ts
ctx.t('Submit');
ctx.t('Hello {{name}}', { name: 'Guest' });
```
