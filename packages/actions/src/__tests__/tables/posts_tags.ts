import { TableOptions } from "@nocobase/database";

export default {
  name: 'posts_tags',
  tableName: 'actions__posts_tags',
  fields: [
    {
      type: 'string',
      name: 'test',
    },
  ],
} as TableOptions;
