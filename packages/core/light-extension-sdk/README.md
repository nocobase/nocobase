# @nocobase/light-extension-sdk

Compatibility facade for the canonical `@nocobase/js-template-sdk` package.

All historical entry points remain available: the package root, `client`, `shared`, `schema`, `schema/server`,
`schema/entry-v1.schema.json`, `typegen`, and `package.json`. New code should import `@nocobase/js-template-sdk`.
