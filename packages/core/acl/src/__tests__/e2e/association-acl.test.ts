import { expect, test } from '@nocobase/test/client';

test.describe('acl check for association field', () => {
  test('view ', async ({ page, mockPage }) => {
    await mockPage().goto();
  });
});

test.describe('acl check for association block', () => {
  test('view ', async ({ page, mockPage }) => {
    await mockPage().goto();
  });
});
